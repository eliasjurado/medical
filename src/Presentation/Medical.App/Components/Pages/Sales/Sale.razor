@inject HttpInterceptorService Interceptor
@inject DialogService DialogService
@page "/venta"

<RadzenStack Orientation="Orientation.Horizontal">
    <RadzenStack Orientation="Orientation.Horizontal" class="rz-w-25">
        <h3>Venta</h3>
    </RadzenStack>
    <RadzenStack Orientation="Orientation.Horizontal" JustifyContent="JustifyContent.End" class="rz-my-5 rz-w-75">
        <RadzenButton Icon="add" ButtonStyle="ButtonStyle.Success" Text="Nueva Venta" Click=@AddRow />
    </RadzenStack>
</RadzenStack>



@code {
    async Task AddRow()
    {
        SaleDto data = await DialogService.OpenAsync<SaleAdd>("Nueva Venta");
        if (data != null)
        {
            // await warehouseService.AddWarehouse(data);
            // selectedItems.Add(data);
            // items = warehouseService.AdminWarehouses.AsQueryable();
        }
    }
}
